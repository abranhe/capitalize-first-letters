/**
 * Capitalizes first letter of each word in string
 * 
 * @param input - the input to be transformed
 * @returns the transfomed string
 * 
 * @example
 * ```
 * import capitalizeFirstLetters = require('capitalize-first-letters');
 * 
 * capitalizeFirstLetters('hello world')
 * // => 'Hello World'
 * ```
  */
declare function transformString(input: string): string

export = transformString;
